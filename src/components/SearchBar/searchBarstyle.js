const searchBarStyle = (theme) => ({
  formControl: {
    minWidth: 120,
  },
  selectEmpty: {
    marginTop: "1.5em",
  },
  boxPosition: {
    marginRight: "20%",
  },
});

export default searchBarStyle;
