const searchBarStyle = (theme) => ({
  root: {
    margin: theme.spacing(5),
  },
  formControl: {
    margin: theme.spacing(1),
    minWidth: 120,
  },
  boxPosition: {
    marginRight: "5%",
    width: "40%",
  },
  searchButton: {
    margin: theme.spacing(1),
    marginLeft: "5%",
  },
});

export default searchBarStyle;
